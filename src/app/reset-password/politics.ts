export interface Politics {
    data: {
        id: number,
        minLength: number,
        capitalLetter: boolean,
        lowerCase: boolean,
        specialChart: boolean,
        numericChart: boolean,
        typeSpecial: string
    }
}

